# ============ Frontend (Frontend/.env) ============
# 前端开发服务器端口（默认 5173，被占用时会自动顺延到下一个可用端口）
VITE_FRONTEND_PORT=5173

# 后端主机与端口（前端 vite proxy 将 /api 转发到该地址）
VITE_BACKEND_HOST=localhost
VITE_BACKEND_PORT=3000
